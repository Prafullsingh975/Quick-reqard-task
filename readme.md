# Project setup

- Clone the project.

## Server

- Do `cd server`
- Run `npm i`
- Add atlas mongoDB url into `dev.env` file.
- Run `npm run dev`

## App

- Do `cd QuickReward`
- Run `npm i`
- Run `npx react-native run-android`
